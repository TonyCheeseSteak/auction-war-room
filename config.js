/* Live sync config.
   Leave this file exactly as-is and the board runs on this browser only.
   Paste your Firebase web config below and every open browser syncs live.

   Get it from console.firebase.google.com:
     1. Create a project (any name)
     2. Build > Realtime Database > Create Database > start in TEST mode
     3. Project settings > Your apps > Web (</>) > register app
     4. Copy the firebaseConfig values into the block below

   These keys are public by design and safe to commit. */

window.FF_SYNC = {
  // apiKey:      "",
  // authDomain:  "",
  // databaseURL: "",
  // projectId:   "",
  // appId:       "",
  room: "draft2026"
};
